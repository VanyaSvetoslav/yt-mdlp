<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="YtMdlp.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:YtMdlp.ViewModels"
    xmlns:models="using:YtMdlp.Models"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:HistoryViewModel />
    </Page.DataContext>

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Downloads" Style="{StaticResource TitleTextBlockStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button Content="Clear" Command="{x:Bind ViewModel.ClearCommand}"/>
            <TextBlock
                VerticalAlignment="Center"
                Text="Click a row to open the file's folder in Explorer."
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <ListView
            Grid.Row="2"
            ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
            SelectionMode="Single"
            IsItemClickEnabled="True"
            ItemClick="ListView_ItemClick">

            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="160"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Filename" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Grid.Column="1" Text="Format"   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Grid.Column="2" Text="Size"     Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Grid.Column="3" Text="Date"     Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:DownloadHistoryEntry">
                    <Grid Padding="0,4" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="160"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Filename}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="1" Text="{x:Bind Format}"/>
                        <TextBlock Grid.Column="2" Text="{x:Bind SizeDisplay}"/>
                        <TextBlock Grid.Column="3" Text="{x:Bind DateDisplay}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
